---
- hosts: server
  become: yes
  become_user: root
  become_method: sudo
  vars_files:
    - ../project.yml
    - ../environments/{{ env }}.yml
  roles:
    - common
    - docker-common
    - influxdb-docker
    - elk-docker
    - grafana-docker
    - riemann-docker

